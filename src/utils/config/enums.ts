export enum KeyNameEnum {
  ESCAPE = 'Escape',
  ENTER = 'Enter',
}

export enum GenderEnum {
  MALE = 'MALE',
  FEMALE = 'FEMALE',
  OTHER = 'OTHER',
}

export enum SortTypeEnum {
  DESC = 'desc',
  ASC = 'asc',
}
